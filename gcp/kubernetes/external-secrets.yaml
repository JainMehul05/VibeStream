# =============================================================================
# external-secrets binding — Google Secret Manager → Kubernetes Secret
# =============================================================================
# Workload Identity binds external-secrets-sa to a Google Service Account
# with `roles/secretmanager.secretAccessor` on the VibeStream-* secrets.
# =============================================================================
---
apiVersion: v1
kind: ServiceAccount
metadata:
  name: external-secrets-sa
  namespace: VibeStream
  annotations:
    # gcloud iam service-accounts add-iam-policy-binding ...
    iam.gke.io/gcp-service-account: external-secrets@REPLACE_PROJECT.iam.gserviceaccount.com
---
apiVersion: external-secrets.io/v1beta1
kind: ClusterSecretStore
metadata:
  name: gcp-secretmanager
spec:
  provider:
    gcpsm:
      projectID: REPLACE_PROJECT
      auth:
        workloadIdentity:
          clusterLocation: REPLACE_REGION
          clusterName: REPLACE_CLUSTER
          serviceAccountRef:
            name: external-secrets-sa
            namespace: VibeStream
---
apiVersion: external-secrets.io/v1beta1
kind: ExternalSecret
metadata:
  name: VibeStream-db
  namespace: VibeStream
spec:
  refreshInterval: "15m"
  secretStoreRef:
    kind: ClusterSecretStore
    name: gcp-secretmanager
  target:
    name: VibeStream-db
    creationPolicy: Owner
  data:
    - secretKey: DJANGO_DATABASE_URL
      remoteRef: { key: VibeStream-cloudsql-url }
    - secretKey: SECRET_KEY
      remoteRef: { key: VibeStream-django-secret }
    - secretKey: JWT_SIGNING_KEY
      remoteRef: { key: VibeStream-jwt-signing-key }
