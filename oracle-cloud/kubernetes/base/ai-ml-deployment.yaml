apiVersion: apps/v1
kind: Deployment
metadata:
  name: VibeStream-ai-ml
  namespace: vibestream-production
  labels:
    app.kubernetes.io/name: VibeStream-ai-ml
    app.kubernetes.io/part-of: VibeStream
spec:
  replicas: 2
  selector:
    matchLabels:
      app.kubernetes.io/name: VibeStream-ai-ml
  template:
    metadata:
      labels:
        app.kubernetes.io/name: VibeStream-ai-ml
        app.kubernetes.io/part-of: VibeStream
    spec:
      containers:
        - name: ai-ml
          image: VibeStream-ai-ml:latest
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 5000
          env:
            - name: SPOTIFY_CLIENT_ID
              valueFrom:
                secretKeyRef:
                  name: VibeStream-secrets
                  key: SPOTIFY_CLIENT_ID
            - name: SPOTIFY_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: VibeStream-secrets
                  key: SPOTIFY_CLIENT_SECRET
          resources:
            requests:
              cpu: "1000m"
              memory: "2Gi"
            limits:
              cpu: "4000m"
              memory: "8Gi"
          readinessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 30
            periodSeconds: 10
          livenessProbe:
            tcpSocket:
              port: 5000
            initialDelaySeconds: 60
            periodSeconds: 20
