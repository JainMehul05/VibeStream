{{- if .Values.podDisruptionBudget.enabled }}
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: {{ include "vibestream-frontend.fullname" . }}
  labels:
    {{- include "vibestream-frontend.labels" . | nindent 4 }}
spec:
  minAvailable: {{ .Values.podDisruptionBudget.minAvailable }}
  selector:
    matchLabels:
      {{- include "vibestream-frontend.selectorLabels" . | nindent 6 }}
{{- end }}
