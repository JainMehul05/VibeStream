apiVersion: v1
kind: Service
metadata:
  name: {{ include "vibestream-frontend.fullname" . }}
  labels:
    {{- include "vibestream-frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "vibestream-frontend.selectorLabels" . | nindent 4 }}
