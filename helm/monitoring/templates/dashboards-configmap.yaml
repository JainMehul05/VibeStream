{{- if .Values.VibeStream.dashboards.enabled }}
{{- /*
Grafana sidecar picks up any ConfigMap with the configured label and mounts
the keys as JSON dashboards. Files are sourced from
../../terraform/modules/monitoring/dashboards.
*/}}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vibestream-monitoring.fullname" . }}-dashboards
  namespace: {{ .Values.VibeStream.dashboards.namespace }}
  labels:
    {{- include "vibestream-monitoring.labels" . | nindent 4 }}
    {{ .Values.VibeStream.dashboards.label }}: "1"
data:
{{- range $path, $_ := .Files.Glob "dashboards/*.json" }}
  {{ base $path }}: |-
{{ $.Files.Get $path | indent 4 }}
{{- end }}
{{- end }}
