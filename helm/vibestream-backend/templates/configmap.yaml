{{- if .Values.configMap }}
{{- if .Values.configMap.data }}
apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "vibestream-backend.fullname" . }}-config
  labels:
    {{- include "vibestream-backend.labels" . | nindent 4 }}
data:
  {{- toYaml .Values.configMap.data | nindent 2 }}
{{- end }}
{{- end }}
