# ArgoCD Application for VibeStream Backend
# Manages backend deployment via GitOps

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: vibestream-backend-production
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: VibeStream

  source:
    repoURL: https://github.com/YOUR_GITHUB_USERNAME/VibeStream.git
    targetRevision: main
    path: helm/vibestream-backend
    helm:
      releaseName: vibestream-backend
      values: |
        environment: production
        replicaCount: 3

        image:
          repository: ${IMAGE_REGISTRY}/backend
          tag: ${IMAGE_TAG}
          pullPolicy: Always

        autoscaling:
          enabled: true
          minReplicas: 3
          maxReplicas: 15
          targetCPUUtilizationPercentage: 70
          targetMemoryUtilizationPercentage: 80

        resources:
          requests:
            memory: "512Mi"
            cpu: "500m"
          limits:
            memory: "2Gi"
            cpu: "2000m"

        monitoring:
          enabled: true
          serviceMonitor: true

        blueGreen:
          enabled: true
          activeEnvironment: blue

  destination:
    server: https://kubernetes.default.svc
    namespace: vibestream-production

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - PrunePropagationPolicy=foreground
      - PruneLast=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m

  revisionHistoryLimit: 10

  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas

---
apiVersion: argoproj.io/v1alpha1
kind: AppProject
metadata:
  name: VibeStream
  namespace: argocd
spec:
  description: VibeStream Application Project

  sourceRepos:
    - 'https://github.com/YOUR_GITHUB_USERNAME/VibeStream.git'
    - 'https://charts.bitnami.com/bitnami'
    - 'https://prometheus-community.github.io/helm-charts'

  destinations:
    - namespace: 'VibeStream-*'
      server: https://kubernetes.default.svc
    - namespace: monitoring
      server: https://kubernetes.default.svc

  clusterResourceWhitelist:
    - group: '*'
      kind: '*'

  namespaceResourceWhitelist:
    - group: '*'
      kind: '*'

  roles:
    - name: admin
      description: Admin role for VibeStream project
      policies:
        - p, proj:VibeStream:admin, applications, *, VibeStream/*, allow
      groups:
        - devops-team

    - name: developer
      description: Developer role for VibeStream project
      policies:
        - p, proj:VibeStream:developer, applications, get, VibeStream/*, allow
        - p, proj:VibeStream:developer, applications, sync, VibeStream/*, allow
      groups:
        - developers
